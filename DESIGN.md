# G-LAB 디자인 기준

출처 : https://studio-noe.github.io/jtr-40j-course/
전체 CSS와 마크업을 역산해 정리했다. 값은 추정이 아니라 실제 소스에서 뽑은 것이다.

---

## 0. 한 줄 요약

**신문 조판이다.** 카드도 그림자도 둥근 모서리도 없다.
구조를 만드는 건 상자가 아니라 **선**이고, 색은 흑백에 노란 형광펜 하나뿐이다.

경기 결과지, 시간표, 성적표의 조판 언어다. 데이터가 많을수록 강해진다.
G-LAB 훈련표처럼 숫자가 빽빽한 문서에 정확히 맞는 이유가 이것이다.

---

## 1. 토큰

```css
:root{
  --ink:#000;        /* 본문, 선, 제목 */
  --paper:#fff;      /* 바탕 */
  --dim:#767676;     /* 라벨, 보조 정보 */
  --hair:#dcdcdc;    /* 행 구분선 */
  --acc:#ffc800;     /* 형광펜. 이 하나가 전부다 */
  --g:minmax(0,1fr); /* 그리드 셀. 표가 넘칠 때 무너지지 않게 */
}
```

- 순흑과 순백을 쓴다. 인쇄물을 흉내내는 조판이라 의도된 선택이다.
- 회색은 딱 두 단계다. `#767676`(글자)과 `#dcdcdc`(선). 중간값을 늘리지 않는다.
- 다크모드가 없다. 종이는 한 장뿐이다.

## 2. 타이포그래피

**Pretendard Variable 하나만 쓴다.** 모노도 세리프도 없다.

```css
font-family:"Pretendard Variable",Pretendard,"Helvetica Neue",Arial,sans-serif;
font-size:15px; line-height:1.5;
font-feature-settings:"tnum" 1;   /* 숫자는 전역으로 고정폭 */
-webkit-font-smoothing:antialiased;
```

| 역할 | 크기 | 굵기 | 자간 | 비고 |
|---|---|---|---|---|
| h1 | `clamp(48px,10vw,120px)` | 800 | `-.045em` | 행간 `.86` |
| 리드 문장 | `clamp(15px,2vw,19px)` | 500 | `-.01em` | `max-width:56ch` |
| 큰 숫자 (fact) | `clamp(24px,4vw,38px)` | 700 | `-.035em` | 행간 1 |
| 섹션 제목 h2 | 17px | 700 | `-.01em` | |
| 소제목 h3 | 13px | 800 | `.04em` | 대문자 |
| 표 본문 | 13.5px | 400~500 | | |
| 표 헤더 th | 10px | 600 | `.1em` | 대문자, `--dim` |
| 라벨 dt | 10.5px | 400 | `.13~.14em` | 대문자, `--dim` |
| 아이브라우 | 11px | 400~600 | `.16em` | 대문자 |

**규칙이 하나 있다.** 크면 자간을 조인다(`-.045em`), 작으면 자간을 벌린다(`.16em`).
큰 글씨는 밀도로, 작은 글씨는 여백으로 읽힌다.

숫자 단위는 본문보다 작게 붙인다. `40.5` 뒤의 `km`는 `.45em` / 굵기 500 / 자간 0이다.

## 3. 선 체계 (핵심)

이 디자인의 전부다. 굵기 세 단계로 위계를 만든다.

| 굵기 | 색 | 쓰이는 곳 |
|---|---|---|
| **2px** | `#000` | 최상위 경계. 아이브라우 하단, 탭 하단, 푸터 상단 |
| **1px** | `#000` | 섹션 제목 하단, 표 헤더 하단, 데이터 블록 상하단 |
| **1px** | `--hair` | 표의 행 구분, 셀 사이 세로선 |

- 표의 **마지막 행만** `1px solid #000`으로 닫는다. 합계 행이라는 신호다.
- 카드 테두리로 쓰지 않는다. `border-radius`는 문서 전체에 **0개**다.
- `box-shadow` 역시 0개다. 깊이를 쓰지 않고 선으로만 가른다.

## 4. 노란색 쓰는 법

**절대 글자색으로 쓰지 않는다.** 항상 검은 글자 뒤에 깔리는 채움이다.

```css
h1 em          { background:var(--acc); padding:0 .06em }   /* 제목 형광펜 */
.two h3 b      { background:var(--acc); padding:2px 7px }   /* 소제목 강조 */
ol.chain li.cp { background:var(--acc); padding:5px 8px }   /* 목록 중 중요한 항목 */
tab[selected]  { border-bottom:4px solid var(--acc) }        /* 활성 탭 밑줄 */
button[pressed]{ background:var(--acc) }                     /* 눌린 토글 */
a.map          { border-bottom:2px solid var(--acc) }        /* 링크 밑줄 */
a.map:hover    { background:var(--acc) }                     /* 호버는 통째로 채움 */
.note          { border-left:3px solid var(--acc) }          /* 주석 */
.area          { fill:var(--acc) }                           /* 그래프 면 */
```

용법이 다섯 개뿐이다. **형광펜 / 밑줄 / 채움 / 왼쪽 선 / 그래프 면.**
이 다섯 밖으로 나가지 않는다.

노랑 위 검정은 대비 12.6:1이라 접근성도 안전하다. 반대로 흰 배경 위 노란 글자는 1.6:1이라 못 쓴다. 규칙이 미학이 아니라 물리다.

## 5. 레이아웃

```css
.wrap    { max-width:1240px; margin:0 auto; padding:0 28px 96px }
.mast    { padding:40px 0 0 }
section  { margin:58px 0 0 }
```

**섹션 머리는 한 줄이다.** 왼쪽에 제목, 오른쪽 끝에 작은 대문자 라벨, 아래 1px 검은 선.

```html
<div class="shead"><h2>고도 프로필</h2><span>CP / CUT-OFF</span></div>
```

**요약 수치는 `<dl>` 4열 그리드.** 위아래를 검은 1px로 닫고 셀 사이만 hair로 가른다. 상자가 아니다.

**표는 `.scroller`로 감싼다.** `min-width:700px`을 주고 넘치면 가로 스크롤. 좁은 화면에서 표를 접거나 줄이지 않는다. 데이터는 데이터 모양으로 둔다.

**브레이크포인트는 두 개.**
- `860px` : 4열 그리드가 2열, 2열 본문이 1열, 아이브라우가 2행으로 재배치
- `430px` : 아이브라우 글자 10px, 부가 요소 숨김

## 6. 모션

**참조 사이트는 모션이 0이다.** 소스 전체에 `transition`도 `animation`도 `@keyframes`도 없다.
호버는 배경이 노랗게 차고, 포커스는 `2px solid #000` 아웃라인이 `offset:2~3px`로 붙는다. 그게 전부다.

### G-LAB이 둔 예외 두 가지

무모션 원칙을 그대로 가져가되, **덮이고 열리는 것 두 개에만** 모션을 허용한다.
색이나 위치가 그냥 바뀌는 것에는 여전히 모션을 넣지 않는다.

| 대상 | 지속 | 이징 |
|---|---|---|
| 주차 상세 시트 (바텀시트 / 사이드패널) | 200ms | `cubic-bezier(.2,.8,.2,1)` |
| 배경 스크림 | 160ms | `linear` |
| 주차 아코디언 펼침 | 200ms | `cubic-bezier(.2,.8,.2,1)` |

**근거.** 시트는 화면 밖에서 들어오는 요소라 어디서 왔는지 보여줘야 방향 감각이 생긴다. 아코디언은 클릭한 자리 아래로 내용이 밀려나므로, 즉시 점프하면 눈이 위치를 잃는다. 둘 다 위계나 상태 전환을 설명하는 모션이지 장식이 아니다.

**구현 주의.** `<details>`는 높이 전환을 지원하지 않는다. `::details-content`와 `interpolate-size`는 `CSS.supports`가 참을 반환해도 실제로는 동작하지 않는 경우가 있어 쓰지 않는다. Web Animations API로 `height`를 직접 애니메이션하고, 진행 중에만 `overflow:hidden`을 건다.

`prefers-reduced-motion: reduce`면 셋 다 즉시 전환으로 떨어진다.

## 7. 데이터 시각화

- SVG 직접 그린다. 차트 라이브러리 없음
- 면은 노랑 채움, 선은 검정 `stroke-width:1.5`
- 모든 선에 `vector-effect:non-scaling-stroke`. 확대해도 선 굵기가 안 변한다
- 보조선은 검정 `stroke-dasharray:2 3` + `opacity:.45`
- 지도 타일에 `filter:grayscale(1) contrast(.92) brightness(1.06)`. 지도까지 흑백으로 끌고 온다
- 지도 핀은 흰 바탕 검은 테두리, 중요한 것만 노란 바탕
- SVG 글자에 `paint-order:stroke; stroke:#fafafa; stroke-width:3px`. 배경 위에서 글자가 뜬다

작은 막대는 트랙까지 그린다.

```css
.dens .track{ width:62px; height:4px; background:#ededed }
.dens .bar  { height:4px; background:var(--acc) }
```

높이 4px다. 대시보드 게이지가 아니라 표 안의 눈금이다.

## 8. 접근성과 인쇄

- `:focus-visible{ outline:2px solid #000; outline-offset:2px }` 전역
- 탭은 `role="tablist"` + `aria-selected`, 토글은 `aria-pressed`
- 인쇄 스타일 있음. 탭을 숨기고 숨겨진 패널을 전부 펼치고 표의 `min-width`를 푼다. 트랙에 종이로 들고 갈 수 있다

---

## 10. 내 조 보기

훈련표는 8개 조 x 최대 8열이라 좁은 화면에서 가로 스크롤이 불가피하다.
한 조만 볼 때는 **표를 눕히지 않고 세운다.** 행 하나를 세로 목록으로 전치하면 폭 문제가 사라진다.

- 위쪽 3칸은 `.facts.three`. 의미를 **질주 / 회복 / 분량**으로 고정한다. 훈련 타입이 바뀌어도 같은 자리에 같은 종류의 숫자가 온다
- 아래는 `.info`. 라벨 왼쪽, 값 오른쪽
- 새 컴포넌트를 만들지 않는다. 둘 다 이미 있는 것이다

컨트롤은 2단이다. `전체 / 내 조` 토글을 누르면 그 아래 조 선택 스트립이 열린다.
선택은 `localStorage`에 남고 `#g=4` 해시가 있으면 해시가 이긴다. 저장소 접근이 막히면 조용히 전체로 떨어진다.

조별 기준 페이스표만 예외로 8행을 유지하고 선택한 조에 연한 노랑을 깐다. 그 표는 자기 조를 고르는 표라서 한 줄만 남기면 쓸모가 없다.

## 9. G-LAB에 적용할 때

### 그대로 가져오는 것

- 선 3단계 체계 (2px 검정 / 1px 검정 / 1px hair)
- 반경 0, 그림자 0, 그라디언트 0
- Pretendard 단일 패밀리 + 전역 `tnum`
- 큰 숫자는 자간 조이고, 작은 라벨은 자간 벌리기
- 표를 `.scroller`에 넣고 `min-width` 유지
- 모션은 시트와 아코디언 두 곳만. 나머지는 0
- 인쇄 스타일

### 바꿔야 하는 것

**노랑을 GSRC 앰버로.** `#ffc800` 대신 `#f9a825`다. 형광펜 용법은 그대로 쓸 수 있다. 앰버 위 검정은 9.58:1로 안전하다.

**다크모드를 버린다.** 지금 G-LAB 페이지는 라이트/다크 양쪽을 지원한다. 이 조판은 종이 은유라서 다크가 성립하지 않는다. 노란 형광펜도 검은 선도 흰 종이를 전제한다.

### 판단이 필요한 지점

**아이브라우를 어떻게 할 것인가.** 이 참조 사이트는 대문자 자간 라벨을 아낌없이 쓴다. `.fact dt`, `th`, `.shead span`, `.info dt`, `.meta dt` 전부 그렇다. 그런데 tasteskill Section 9.F는 아이브라우를 3섹션당 1개로 제한한다.

충돌이 아니라 문맥 차이다. tasteskill이 막는 건 **섹션 제목 위에 얹는 장식용 라벨**이고, 여기 쓰인 건 **표의 열 이름과 데이터 라벨**이다. 후자는 기능이다. G-LAB에서도 표 헤더와 `dt`에는 쓰고, 섹션 제목 위에는 안 쓴다.

### 가져오지 않는 것

참조 사이트의 `<title>`은 이렇다.

```
JTR 40J 코스 정보 · 모의 훈련코스 — 2026.09.19 장수
```

가운뎃점과 em dash가 들어 있다. 우리 글쓰기 규칙은 둘 다 금지다. `/`로 바꾼다.

```
G-LAB 훈련표 / GSRC 구파발 러닝랩
```
